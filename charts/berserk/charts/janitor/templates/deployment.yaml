apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "janitor.fullname" . }}
  labels:
    {{- include "janitor.labels" . | nindent 4 }}
    component: backend
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "janitor.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "janitor.selectorLabels" . | nindent 8 }}
        component: backend
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: janitor
          image: {{ include "janitor.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy | default .Values.global.imagePullPolicy | default "Always" }}
          command: ["/janitor_bin"]
          args:
            - "--config"
            - "/config/config.yaml"
          ports:
            - containerPort: 9502
              name: health
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: RUST_BACKTRACE
              value: "1"
            - name: JANITOR_STORAGE_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.s3Credentials.secretName }}
                  key: {{ .Values.global.s3Credentials.accessKeyIdKey }}
            - name: JANITOR_STORAGE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.s3Credentials.secretName }}
                  key: {{ .Values.global.s3Credentials.secretAccessKeyKey }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            - name: cache
              mountPath: /tmp/segment-cache
      volumes:
        - name: config
          configMap:
            name: {{ include "janitor.fullname" . }}-config
        - name: cache
          emptyDir:
            sizeLimit: {{ .Values.cache.size }}
