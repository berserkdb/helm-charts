{{- /*
Headless companion to the primary `query` Service — the gateway's `grpc_routes`
upstream resolves this name so it can hold one connection per query pod. The
rationale and the deliberate omissions live on the shared define.
*/ -}}
{{ include "berserk-common.service.headless" . }}
